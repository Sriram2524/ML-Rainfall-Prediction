Flask==3.1.1
numpy==2.4.0
pandas==2.3.0
scikit-learn==1.7.0
joblib==1.5.1
gunicorn==22.0.0
matplotlib==3.10.3       # only if generating plots
xgboost==...             # if model uses it
lightgbm==...            # if model uses it
pickle-mixin==...        # usually not needed